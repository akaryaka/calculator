import { ref } from "vue";

export const mathState = {
    btnValue : ref(''),
    mathAction : ref(''),
    mathValue1 : ref(''),
    mathValue2 : ref(''),
    mathEqual : ref(0),
    mathOutput : ref(''),
}

// export {btnValue}
